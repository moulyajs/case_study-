/*
#include <stdio.h>

int main()
{

    char array[100]; // array to hold input string

    int i = 0; // counter for array index

    printf("Enter a string: ");

    // read character by character
    while ((array[i] = getchar()) != '\n')
    {
        i++;
    }

    array[i] = '\0'; // add null terminator

    printf("You entered: %s\n", array);

    return 0;
}
*/